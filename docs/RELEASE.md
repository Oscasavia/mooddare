# Release checklist and audit

## Implemented

- Android on-device face detection with custom, face-masked photo smoothing; light/warmth controls; original comparison; shared JPEG export path for gallery/share/feed.
- Android live photo/video lens carousel: Original, Soft, Glow, Wide eyes, Sculpt, Studio, Rosy and My look. Presets have strength/compare controls; My look has independent Smooth/Eyes/Face controls plus compare/reset, retained for the camera session. Native CameraX/ML Kit/OpenGL pipeline applies smoothing, color, eye enlargement and face slimming to preview, photos and recorded MP4 video with microphone audio.
- Android opens directly into a camera with floating controls and a repeating lens wheel. The viewfinder is centered vertically and horizontally. Default 9:16 framing can be changed to 3:4 or Full; the viewfinder and saved photo/video share the chosen ratio. Tap for a photo, hold/release for video, slide up to lock, tap to stop. Strength/comparison controls appear on demand. Camera startup error/retry states, front camera default, lifecycle cleanup and the native 30-second limit remain.
- Capture review emphasizes the media and Post action. Photo adjustments float inside the photo’s rounded frame without resizing it, with a repeating icon wheel, selected-effect label, one slider at a time, reset and comparison. Effect icons and the adjustment toggle have no tap splash; Save/Share live in an overflow menu. Video review has accessible play/pause and scrubbing.
- Android live photo flash: manual off/on, white screen and temporary window brightness for selfies, brief CameraX torch illumination for rear cameras with an LED. Capture waits for exposure and new frames. Native timeout, pause/close and Dart cleanup restore lighting; video is unaffected.
- Selected-mood screen uses a mood-colored background, emoji badge and focused dare card, with a compact shuffle action and a fixed Open camera button. Long dares and enlarged text scroll without hiding the main action; duplicate camera launches are guarded.
- Material 3 dark theme, responsive auth/discovery/editor screens, real profile counts and milestones; removed fake social stats, subscriptions, notification switches and unused seed tools.
- Actual sign-out, Firebase auth errors, guest-to-account linking on signup, username reservations, avatar selection/cropping/upload and profile bio.
- Bounded feed queries, cached author/thumbnail futures, video pause on navigation/backgrounding, like transactions, post deletion confirmation, reporting and blocked accounts.
- Explicit post authorization, consistent media metadata, stable IDs for retrying posts, release Internet permission and no debug signing fallback for release.
- Local Firestore/Storage access-rule tests, image-processing tests, widget tests, Android integration test and GitHub CI configuration.

## Current comments rollout and remaining profile migration

On 2026-09-19, the live Firestore policy was inspected: it allowed every authenticated user to read/write every document. The comments rollout deploys `firestore.compat.rules` using `firebase.compat.json`. Posts now enforce author identity, immutable content, self-only likes and owner-only deletion; nested comments enforce parent existence, author identity, 1–500 nonblank characters, server timestamps, and deletion by comment/post owner. The follow-up rules allow author-only text edits with server editedAt timestamps and caller-only likes, preserving immutable author/creation metadata. Both the compatibility policy and full target policy pass the comment/mood/cleanup emulator tests. Existing non-post permissions are preserved to avoid breaking unmigrated profiles, and remain a release gate. Storage policy was not changed in this rollout.

Deploy this compatibility configuration until the migration below is complete:

```sh
firebase deploy --project mooddare --config firebase.compat.json --only firestore
```

New post metadata is optional for older clients and paired for newer ones (`moodId`, `moodName`). Feed filtering uses the `moodId`/`expiresAt` composite index. Existing posts without mood metadata are available under All moods; they are not guessed or backfilled. Comment account cleanup uses the collection-group author index. Post deletion removes comment batches before deleting its parent. Account deletion removes the caller's comments on other posts too. Trusted server cleanup is still needed for interrupted deletion and races with newly arriving comments. Comments stream the latest 100; older-comment pagination, spam throttling and comment reporting are future moderation work.

The full `firestore.rules` profile policy and username collection must be rolled out together after these steps:

1. Export/back up existing Firestore data. Audit current rules and enabled Auth/Storage services.
2. Migrate public `users/{uid}` documents: remove `email` and any other private fields (Firebase Auth remains the email source); populate `id`, `createdAt`, and lowercase `username_lower`; keep only fields allowed by `validProfile`. Resolve usernames that do not match the new 3–20 letters/numbers/underscore format.
3. Check ALL usernames case-insensitively for collisions, including legacy profiles without helper fields. Create matching `usernames/{lowercase}` documents with `{uid}` using trusted admin tooling. Resolve collisions manually before opening signups. Client compatibility queries are not a replacement for this migration.
4. Test a migrated staging copy, including old posts/avatar paths, signup, guest linking, profile rename, likes, block/unblock, account deletion and failed uploads.
5. Deploy reviewed Firestore rules, Storage rules and indexes to the intended project. There is no automatic production deployment in CI. Existing rules may deny new username/report/block operations until rollout.

Do not deploy the policy onto unmigrated profiles: old public email fields will otherwise remain readable, and profile updates may be rejected.

## Moments interaction verification

Validation: 124 Flutter unit/widget tests and all 19 Firebase emulator tests pass. The Settings/comment-deletion update does not change the schema, rules or indexes. The updated native Moments regression passes with real video playback, aligned controls, likes-sheet playback interruption, comment editing and comment likes. The seven unchanged camera/editor/catalog integration scenarios passed in the preceding update. New regression coverage includes long-press likes in both views without accidental likes, lazy profile fetching, retries/missing/blocked profiles, commenter profile navigation with draft preservation, compact author spacing, full-width comments and inline heart/count alignment at large text sizes on a 320-pixel phone. Existing coverage includes author-only edits, like preservation, edit cancellation/drafts, aggregate counts, compact formatting, profile navigation and safe-area alignment. Flutter analysis is clean. Both feed and full-screen buttons opened Android’s real share chooser on the emulator and were dismissed without sending. Deployed mood/comment indexes reached READY. Live multi-account commenting/editing/liking still needs physical-phone acceptance. Aggregate comment totals refresh after local interaction and once per active minute; they are not a per-card live subscription. Comment likes currently use the same bounded UID-array design as post likes, with a 10,000-like limit; large-scale engagement needs a server-maintained counter/reaction design.

The comments/mood-filter/sound update adds regression coverage for sliding comments from feed and viewer, posting/deletion, load and send retries, stable retry IDs, blocked comments, unsigned viewers, keyboard insets, large text on a 320-pixel phone, native share arguments/errors, filtered and empty feeds, Back refresh/exit timing, session-wide mute and playback interruption. The native Moments test exercises real MP4 playback, mute controls below a tall status bar, comments pausing/resuming playback and shared viewer navigation. The seasonal capture test checks mood metadata reaches photo review. Firebase emulator checks run against both policies and cover forged authors, missing parents, whitespace/oversized comments, unauthorized edits/deletes, post ownership, paired mood metadata and account-cleanup queries.

Single and double media taps now resolve in the same gesture detector; the native regression also checks single-tap pause after closing the social sheets.

The liked-by panel reads the post’s current UID array when opened, then fetches visible/nearby profiles lazily and caches them for that sheet. It waits for blocked-account data before showing the list. Comments retain their avatars/usernames as profile links; dragging down or Back dismisses the sheet, while Cancel is available only during editing.

## Settings and comment deletion verification

Seven additional comment-deletion scenarios cover author and post-owner deletion from both feed and viewer, totals refreshing, failed deletion and retry, draft/like preservation, deletion during editing, duplicate-request protection and dismissing a pending operation. The native Moments regression deletes an edited/liked comment and confirms the empty state before resuming video. Both Firestore policies reject unauthenticated/unrelated deletion and allow comment/post owners to delete; tests verify the document is removed. Production user comments were not deleted during testing.

Settings tests cover bottom placement of account actions, confirmation/cancellation, failures, retries, disabled actions/Back while pending, password validation and exact input, reauthentication before password updates, rejected credentials never updating a password, reset destination, guest/Google account handling, sign-out provider failures, FAQs, support draft URI encoding/copy fallback, share origin/error recovery, native channel arguments and 320-pixel layouts at 2× text scale. Real account deletion/password changes were not performed against a personal production account; these operations are tested with injected accounts/services. Android native version display, app notification settings and the app-share chooser were visually verified. Sharing was dismissed without sending. Swift syntax validation passes; the iOS handoff still needs an iPhone build/device check.

The support address currently uses the owner's approved Gmail address. The app-sharing URL is an explicitly labelled `.example` placeholder, not a download destination. Replace both before public release as described in README. Push notifications remain unimplemented; no nonfunctional category switches were added. Platform settings references: [Android notification settings](https://developer.android.com/reference/android/provider/Settings#ACTION_APP_NOTIFICATION_SETTINGS) and [iOS notification settings](https://developer.apple.com/documentation/uikit/uiapplication/opennotificationsettingsurlstring).

## Welcome, mood search and profile toolbar verification

Nine new regression scenarios cover both welcome routes with guest entry absent at normal and 2× text scale, real Google image decoding and provider-button behavior in both auth modes, profile validation/save errors/retry/pending-operation protection and successful navigation, disabled Save during failed/missing/loading profiles, toolbar visibility with a keyboard and large text, mood-search matching/clear/empty/selection/reset, selected checkmarks, local-only typing and dismissal without feed refresh. All 124 Flutter tests pass and analysis is clean. The updated Android Moments test passes with mood search before real photo/video playback, comments and deletion. The 19 Firebase rules tests passed in the preceding Settings update; no backend rules/schema changes are required here. No production profile changes or sign-ins are performed by these tests.

The welcome collage is drawn in Flutter and works offline. The bundled Google asset is sourced and attributed in `assets/branding/README.md`. Guest entry removal only affects the welcome screen; it does not delete or lock out existing anonymous accounts.

## Store and backend release gates

- Choose and register production Android/iOS bundle IDs. Android still uses `com.example.mooddare` to preserve the existing Firebase configuration. Replace Firebase config files together with any identifier changes.
- Supply `android/key.properties` and a private release keystore; both are ignored. Release no longer falls back to the debug key. Configure iOS signing and confirm the current Play/App Store SDK requirements before submission; this project has not been migrated to the newest Flutter/toolchain.
- Publish real privacy/terms/support information and complete store data disclosures. The in-app data explanation is not a full privacy policy.
- Arrange a moderation workflow for `reports` and an appropriate user-content policy. Blocking currently hides that author's feed posts and comments for the blocking user; it is not a server-enforced access ban.
- Firebase media download URLs are bearer links: recipients can access shared files until those files/tokens are revoked. The 24-hour feed timer does not delete files or revoke URLs.
- Add trusted server cleanup for interrupted account deletions and orphaned uploads. Current account deletion cleans up from the client and keeps Auth until last, but is not atomic across Firebase services. Reauthenticate before deleting a non-guest account. Users should keep the app open and retry after an interruption. Server-side cleanup is needed for a reliable production deletion SLA.
- Add server-enforced abuse controls / App Check as appropriate. Current rules enforce ownership and field constraints, not posting-rate limits. Stats aggregate the user's posts client-side; move aggregates server-side and add pagination beyond 60 posts as usage grows. Likes still use a bounded array, not a scalable per-like collection.
- Measure Firebase usage and set budget alerts; the camera itself has no paid inference service, but Firebase Storage/Firestore/hosting remain separate services.

## Future subscriptions and seasonal content

The catalog currently has Free, Daring and Epic tiers. Daring/Epic are locked **Coming soon** previews; no billing SDK, store products, purchase flow or entitlement delivery exists. Older Gold/Diamond values remain compatible aliases for Daring/Epic. Preview cards and direct-route guards do not secure content: the current Firestore policy allows signed-in reads of `dares`. Before selling access, separate public catalog metadata from protected paid dares, verify store receipts on a trusted server, enforce entitlements on the backend, and test purchase/restore/expiration/refund/account-linking flows. Do not unlock premium access from a local boolean.

Christmas and New Year are free, year-round collections with themed dares. Additional holidays can use the optional `season` metadata independently of tier. Regional calendars, automatic activation windows, localization and annual content scheduling remain future work.

## Device acceptance testing

Mesh-guided shaping: eye enlargement uses the tracked eye contours, adapts its radius to each eye, and eases off as eyelids close. Four bounded jaw guides follow the left/right cheek and lower-jaw contours rather than a broad face-box warp. Guides use image-height coordinates aligned to the eye line, preserving head tilt, aspect ratio and selfie mirroring. They share stabilized geometry with makeup and pause when a valid mesh is missing. Original and independent custom amounts remain supported; no new lens presets were added. Existing frontal-pose limits still apply.

Shaping validation: 140 Flutter tests and 24 native JVM tests pass; full-repository formatting and analysis pass. Ten distinct Android camera scenarios passed across the full run and targeted reruns. New coverage checks blink suppression, head-tilt/aspect geometry, central-mouth protection, distant-background preservation, mirrored shaping, mesh loss, still/preview parity and fresh still detection for Eyes/Face/Rosy at 600 and 2048 pixels. The public test portrait was visually inspected. Existing video tests verify rendered effects, AAC audio and the 30-second limit. Two initial emulator runs timed out switching cameras after a flash photo; two traced reruns and the final run without tracing passed. No cause was established, so physical camera switching/retake and lifecycle stress remain acceptance checks. The user subsequently confirmed alignment while blinking/smiling/tilting, photo/video output and camera switching after flash on the Samsung. Broader testing with glasses, facial hair and different users remains necessary. The shader transforms only local displacement, preserving exact input coordinates outside the shaping areas even at mobile GPU precision.

Rosy photo follow-up: Samsung detector-status logs showed valid still landmarks but no still mesh even though the preview mesh was valid. Photos now try live-sized analysis, a padded crop around the face detected in that exposure, and finally the original still. Crop contours map back into full-photo coordinates; saved resolution is unchanged and stale preview geometry is never reused. A new native regression exercises fresh still detection (rather than the cached-preview fixture) at 600- and 2048-pixel heights, both selfie orientations, and editor normalization. The larger fixture reproduces the missing mesh and passes via the crop retry. Validation: all 139 Flutter tests, 19 JVM tests and five Android photo/makeup scenarios pass; full-repository Dart formatting and analysis pass. The user confirmed Rosy lip tint remains visible in the editor on the physical Samsung; the matching detector-status log shows a valid mesh on the 1080 × 1440 photo using the first analysis attempt. The GitHub formatting failure was an indentation error in the profile-navigation test; it is corrected, and formatting must be checked across `lib test integration_test`, not only changed files.

Mesh/Rosy validation: 139 Flutter tests, 16 native JVM tests and eight Android camera integration scenarios pass; analysis and formatting are clean. Geometry tests cover malformed contours, mesh-to-face matching, bystander rejection, consistent contour sampling, synchronized temporal filtering, identity changes, missing geometry and expiry. Pixel tests verify the actual skin/lip masks, open-mouth and feature exclusions, mirrored lip tint placement, unchanged background, Original bypass and matching higher-resolution still output. Video tests include makeup in the GPU-to-MP4 comparison and verify audio, the 30-second limit, retake and interruption recovery. The public-domain portrait was visually compared with the rendered Rosy image. Warm fixture mesh/detection completion measured about 42–74 ms; the live emulator camera preview measured about 29.6 fps with Original selected. These are separate measurements, not proof of sustained mesh performance or alignment on a moving face. Physical Samsung checks should include speaking, smiling, quick movement, glasses, varied lighting and sustained recording. Rosy is the first lip-tint/blush look; the subsequent mesh-shaping update replaces the original landmark-only eye/jaw guides.

Custom-look validation: 138 Flutter tests and four Android lens/video integration scenarios pass; analysis and formatting checks are clean. Tests cover independent bounded Smooth/Eyes/Face values, Compare without losing edits, Reset, preset/camera switching, native capture/recording handoff, and recovery from capture errors. Narrow and foldable layouts are exercised with 2× text sizing and unchanged viewfinder geometry. Native tests verify custom GPU output and original bypass, recording with the custom controls, hiding controls while recording, and retaining amounts after retake. Custom settings currently live for one camera session; reopening the camera starts with Original and zero custom amounts. This update adds control over the existing effects, not new face-mesh tracking or makeup rendering.

The latest Android lens refinement adds ten JVM regression tests for identity retention, bystander exclusion when the tracked face is occluded, jitter reduction, motion response, interpolation, missing IDs, identity/spatial jumps, stale/out-of-order results, loss/reacquisition, pose attenuation and invalid landmarks. These tests now run in GitHub CI alongside the Flutter suite. The renderer preserves at least 28% of the original signal inside the smoothing blend, increases edge preservation, expands eye/brow protection and reduces maximum eye/jaw warp. These are bounded rendering changes, not proof of aesthetic quality: physical-phone acceptance across users and lighting remains required.

Lens-refinement validation: all 134 Flutter tests, 10 native tracking unit tests and 6 Android camera/photo/video integration scenarios pass; Flutter analysis is clean. The live preview measured 29.6 fps at 960 × 1280 on the emulator. Native checks cover GPU effects, original/no-face bypass, framing/mirroring, capture/retake, camera switching, recording with audio, the 30-second limit, playback and interruption recovery. Physical-phone performance and perceived quality are separate acceptance checks.

Still-photo quality validation: all 134 Flutter tests and six Android scenarios across `high_quality_photo_test`, `live_beauty_test` and `live_video_test` pass; analysis is clean. Fine-detail fixtures distinguish the larger capture from an enlarged preview, verify 9:16/3:4 crops and selfie mirroring, retain 2048-pixel editor input, and confirm that offscreen rendering leaves the preview intact. Sensor tests cover front/rear capture and cancellation followed by a new session. The emulator front camera captured 1440 × 1920 versus its 960 × 1280 preview; the rear sensor supplied 960 × 1280 for both. Preview delivery remained about 29.5 fps. Check shutter latency, exposure/flash timing, memory use, lens alignment during movement and image quality on the Samsung before treating this as physical-device acceptance. Video dimensions remain unchanged.

Confirm crop appearance, comments between two accounts, and sharing real posted photos/videos on the physical phone. Automated checks and backend deployment status are recorded above.

The borderless mood grid was visually checked at 344 logical pixels wide. With Android's user rotation set sideways, the app retained `SCREEN_ORIENTATION_PORTRAIT` and rotation 0; the emulator settings were restored afterward. The iOS orientation plist passes syntax validation, but no iOS device/build validation has been performed. The app requests portrait-up at startup and in native launch settings; platform overrides on large screens still require responsive layouts.

Catalog tests cover legacy and explicit tiers, malformed data, duplicate merging, offline/time-out fallback, retry, search/filter intersections, premium preview access, seasonal dares, two-column phone layouts at 320/344/390 logical pixels with text scales up to 2.0, borderless field/pill styles, large-text layouts, and navigation from a seasonal mood through live capture and photo review. The live tests cover real GPU pixel changes for eyes/jaw/smoothing, unmodified background, original bypass, no-face bypass, RGBA color order, upright/mirrored exports, matching centered photo/video crops, continuing frames, capture/retake, camera switching and rapid background/resume. Gesture/layout tests cover tap-versus-swipe, hold/release, slide-to-lock, slow recorder startup, cancellation, automatic-stop reset, ratio switching with matching viewfinder/export settings and enlarged text on narrow/foldable layouts. Video tests verify baked effects against the GPU still, H.264/AAC tracks with matching durations, playback, the 30-second cutoff, repeated recording and interrupted-clip recovery. These checks do not exercise store signing. The user separately confirmed physical-phone live photo lenses and, on the preceding build, posting, photo edits, video playback and audio. The user also confirmed filtered video on the physical phone before the camera UI redesign. The user confirmed the gesture/layout redesign on the physical phone. The user confirmed the centered camera, review redesign and icon wheel on the physical phone. The user confirmed the floating photo controls. The user confirmed the selected-mood redesign on the physical phone. The user also confirmed the mood-list redesign and portrait update on the physical phone. Flash still needs dark-room exposure checks, rear LED checks, and brightness-restoration checks after interruptions on real hardware. The emulator verifies front-screen flash capture and cleanup but cannot establish real LED illumination quality.

Live camera measured about 29.5 fps at 960 × 1280 on the emulator. Physical-phone FPS, tracking latency, power consumption and thermal throttling remain acceptance checks. CameraX RGBA conversion/copy and downscaled face detection use CPU; the image effects run on the GPU. Frame queues are bounded. This is a portrait, single-face implementation: face-mesh contours guide eye/jaw warping and constrain smoothing and makeup; geometric landmarks identify and validate the tracked face. It does not provide semantic skin segmentation or 3D AR masks. Test large head turns, lost/reacquired faces, overlapping faces, glasses, facial hair and movement. Live photos use a dedicated CameraX exposure with the same sensor viewport and GPU shader, capped at a 2048-pixel longest edge; face landmarks are re-detected on that exposure. Camera stream limitations may require preview capture as a fallback. Photos preserve the selected centered crop and mirrored selfie appearance; test folded/unfolded layouts and orientation before extending landscape support. Live lens effects are baked into captured photos, so the later editor cannot recover a pre-lens original.

Automated Android tests prove integration, not aesthetic quality across users. Validate smoothing with consenting test users across skin tones, lighting, glasses, facial hair and movement before marketing it as a beauty feature. Tracking starts with the largest detected face and keeps that person while their ID remains visible. Missing usable landmarks or unsupported angles disable the effect instead of transferring it to a bystander. Effects attenuate near the existing frontal-pose limits; profile faces remain unsupported. Neither the contour mask nor the geometric fallback is semantic skin segmentation. Mesh matching can fail on small, obscured or strongly turned faces; makeup then turns off. The face-mesh dependency is a bundled, pinned beta SDK. Check talking/open mouths, teeth, glasses, facial hair and quick head movements with Rosy on physical devices before a public release.

Check: capture/retake, camera flipping, permission denial/regrant, background/resume while recording, no-face handling, all slider combinations, processed gallery/share/upload equality, offline/retry, long text, large text sizes and screen readers. Record 30 seconds and verify audio/video sync. Test a low-end and a recent physical Android phone.

Live video uses the GPU output surface with MediaRecorder H.264/AAC encoding, capped at 720 pixels on the short edge and 1280 on the long edge, targeting 4 Mbps video and 96 kbps mono audio. Native limits stop at 30 seconds or 28 MiB. The app finalizes recordings when it loses focus and reviews interrupted clips on resume while the process survives; force-kill/crash recovery is not implemented. Phone acceptance must include audible speech/lip sync, incoming interruptions, microphone denial/regrant, tap/hold/release and slide-to-lock on real touch hardware, long-clip playback and uploading the filtered clip. Emulator track-duration checks do not prove physical-device lip sync.

No iOS build/device validation has been completed. Native iOS face detection and live beauty video processing are not implemented. Before iOS release, configure/test all permissions, Google sign-in, gallery sharing, account deletion and build/signing; verify minimum OS versions with the installed plugins.

## Source backup

A snapshot of the original source/configuration was saved locally at `/private/tmp/mooddare-before-refresh.tar.gz` before editing. It is not a durable backup or part of GitHub. The repository remote is `https://github.com/Oscasavia/mooddare.git`.
